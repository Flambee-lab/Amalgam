import { InteractiveCapabilityModule } from "@/modules/interactive-capability";

export default function DemoPage() {
  return (
    <main className="min-h-screen w-full max-w-[100vw] overflow-x-hidden bg-amalgam-canvas">
      <InteractiveCapabilityModule />
    </main>
  );
}
